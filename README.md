# No Tengo Fichas

Hub central de herramientas para partidas de cartas. React + Vite + Tailwind CSS.

Las apps del hub se definen en `src/data/apps.js`; añadir una app nueva no requiere tocar componentes.

## Desarrollo local

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
